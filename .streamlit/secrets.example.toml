# This project currently does not require Streamlit secrets.
# Keep real secrets in .streamlit/secrets.toml, which is ignored by git.
#
# Example only:
# [example]
# api_key = "replace-with-your-own-local-secret"
