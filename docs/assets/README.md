# Assets

This directory is reserved for future public portfolio assets such as diagrams or screenshot variants.

Current screenshot files live under `docs/screenshots/` and are referenced only after they exist in the repository.

Do not place secrets, local cache files, real `data/ticks` snapshots, SQLite files, or private local paths in this directory.
